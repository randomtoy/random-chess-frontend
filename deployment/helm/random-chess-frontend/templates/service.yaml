apiVersion: v1
kind: Service
metadata:
  name: {{ include "random-chess-frontend.fullname" . }}
  labels:
    {{- include "random-chess-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "random-chess-frontend.selectorLabels" . | nindent 4 }}
